source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in GeoCompile.gemspec
gemspec

group :development, :test do
  gem 'bixby'
  gem 'coveralls', require: false
  gem 'pry'
  gem 'pry-byebug'
end
